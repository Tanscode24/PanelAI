'use client'
import { useState } from 'react'
import { AppShell } from '@/app/page'
import { Check } from 'lucide-react'
export default function ProfilePage(){const [saved,setSaved]=useState(false);const [form,setForm]=useState({fullName:'Alex Morgan',email:'alex@example.com',title:'Senior Product Designer',location:'San Francisco, CA',bio:'Designer and systems thinker who loves turning complex problems into simple experiences.',targetRole:'Senior Product Designer',experience:'5+ years'})
 const update=(key:keyof typeof form,value:string)=>{setSaved(false);setForm(v=>({...v,[key]:value}))}; return <AppShell><div className="page-header"><div><div className="eyebrow">Account</div><h1>Profile</h1></div><button className="button button-primary" onClick={()=>setSaved(true)}><Check size={16}/> Save Changes</button></div>{saved&&<div className="success-banner">Profile updated successfully</div>}<section className="setup-card profile-form"><div className="profile-avatar">AM<button className="button button-ghost">Change Photo</button></div><div className="form-row"><label>Full Name<input value={form.fullName} onChange={e=>update('fullName',e.target.value)}/></label><label>Email<input type="email" value={form.email} onChange={e=>update('email',e.target.value)}/></label></div><div className="form-row"><label>Professional Title<input value={form.title} onChange={e=>update('title',e.target.value)}/></label><label>Location<input value={form.location} onChange={e=>update('location',e.target.value)}/></label></div><label className="textarea-label">Bio<textarea value={form.bio} onChange={e=>update('bio',e.target.value)}/></label><div className="form-row"><label>Target Role<input value={form.targetRole} onChange={e=>update('targetRole',e.target.value)}/></label><label>Experience Level<select value={form.experience} onChange={e=>update('experience',e.target.value)}><option>Entry level</option><option>Mid-level (3–5 years)</option><option>5+ years</option></select></label></div></section></AppShell>}
